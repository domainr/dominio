AppAssistant = function(){}

AppAssistant.prototype = {
	/*
	handleLaunch: function(params){
		if (params){
			var sc = Mojo.Controller.getAppController().getStageController(this);
			var launchScene = (function(stageController) {
				stageController.pushScene("main", params.query);
			}).bind(this);
			launchScene(sc);
		}
	}
	*/
}